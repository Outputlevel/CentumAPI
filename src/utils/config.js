import 'dotenv/config';
export const {
    PORT,
    DEV_MODE,
    DEBUG_MODE,
    AUTH_KEY,
    AUTH_HEADER,
    WOO_AUTH_DEV,
    WOO_AUTH_PROD,
    WOO_USERNAME_DEV,
    WOO_USERNAME_PROD,
    PUBLIC_HEADER,
    PUBLIC_KEY,
    CONSUMER_ID_HEADER,
    CONSUMER_ID_VALUE,
    API_URL,
    WOO_URL,
} = process.env;


